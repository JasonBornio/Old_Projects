using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera : MonoBehaviour
{
    GameObject player;
    GameObject temp;
    bool started = false;

    void Update()
    {
        //if(GameObject.Find("Ground2D").GetComponent<train>().best){
        //    player = GameObject.Find("Ground2D").GetComponent<train>().bestBot;
        //    started = true;
        //    transform.position = new Vector3(player.transform.position.x, transform.position.y, transform.position.z);
        //}
        
    }
}
